#include <ctype.h>
#include <string.h>
#include "FoodItem.h"

FoodItem* find_search_term(char*, FoodItem*, unsigned*);
void link(FoodItem*, FoodItem*);
FoodItem* find_product_id(char*, FoodItem*, unsigned*);
